
SELECT *;
 FROM PRECHECK;
 WHERE PRECHECK.ADM_PRINT <> "Y" AND NOT DELETED() AND METER="D" AND (EMPTY(RP_FINISH) OR EMPTY(APPROVED) OR EMPTY(RCL_FINISH));
 INTO CURSOR RPTADM; 
 ORDER BY order_no
REPORT FORM REPAIRS\RPTADM.FRX TO PRINTER PROMPT PREVIEW
